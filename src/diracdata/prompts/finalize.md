You ran out of step budget before calling finish. Compose the BEST-EFFORT FINAL ANSWER now, using ONLY
what is already in your WORKING MEMORY below -- the plan, the verified facts, the stored query results
(with their result_ids), and any sub-agent findings folded in. Do NOT ask for more tools; there are none.

Write the answer to the ORIGINAL GOAL directly: the numbers in plain language, organized the way the
question asked (e.g. the driver decomposition, then the ranked movers per dimension). Use only numbers
that appear in working memory -- never invent one. If some part of the question could not be completed,
state briefly what is missing rather than omitting it silently. End with a one-line CHECKS note listing
the result_ids the figures came from. Output ONLY the answer text.
