"""Per-turn agent RUNTIME state: WorkingMemory + Plan (the analyst's scratchpad) and the
ResultStore (materialized, citable query results). Distinct from diracdata.memory, which is the
PERSISTENT experiential memory reused across turns."""
